@model IEnumerable<TrialNetCore1.Models.Book>
@*<!DOCTYPE html>
    <html>
        <head>
            <meta name="viewport" content="width=device-width"/>
        </head>
        <body>
            <h2>@ViewBag.Title</h2>
            @foreach (var book in Model)
            {
            <div>
                <h2>@book.Judul</h2>
                <h3>@book.Penulis</h3>
            </div>
            }
        </body>
    </html>*@

@*@{
    Layout = "_Layout";
}*@

<h2>@ViewBag.Title</h2>
@foreach (var book in Model)
{
<div>
    <img src="@book.Gambar" alt="" style="height: 250px;width: 200px" />
    <h2>@book.Judul</h2>
    <h3>@book.Penulis</h3>
</div>
}